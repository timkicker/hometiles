<?xml version="1.0" encoding="utf-8"?>
<!--
  debug build only: the probe call.

  what stands here never reaches a delivered program - AGP merges this file into the debug
  build alone. a way to fake a call does not belong in the hands of somebody who merely
  installed the app.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      only so the probe can pick up itself instead of ringing. a program allowed to answer
      calls is something other than a home screen - so the permission stands here and not in
      the main manifest.
    -->
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />

    <application>

        <service
            android:name=".phone.probe.ProbeConnectionService"
            android:exported="true"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>

        <activity
            android:name=".sms.probe.ProbeMessageActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <activity
            android:name=".phone.probe.ProbeActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay" />

    </application>
</manifest>
